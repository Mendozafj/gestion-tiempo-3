<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <header>
    <h1>Gestión del Tiempo</h1>
  </header>

  <div class="container">
    <!-- Título principal -->
    <h1>Bienvenido a <%= title %>
    </h1>
    <p class="subtitle">Tu aplicación para gestionar tu tiempo.</p>

    <!-- Descripción breve -->
    <div class="intro">
      <p>
        Con esta aplicación, puedes gestionar tus actividades, categorías, hábitos y proyectos de manera eficiente.
        ¡Organiza tu tiempo y alcanza tus metas!
      </p>
    </div>

    <!-- Botones de navegación -->
    <div class="actions">
      <% if (user) { %>
        <!-- Opciones para usuarios autenticados -->
        <a href="/dashboard" class="button">Ir al Dashboard</a>
        <% } else { %>
          <!-- Opciones para usuarios no autenticados -->
          <a href="/auth/login" class="button">Iniciar Sesión</a>
          <a href="/auth/register" class="button">Registrarse</a>
          <% } %>
            <!-- Enlaces para ver el tiempo usado por categoría y proyecto -->
            <a href="/categories/time-used" class="button">Tiempo por Categoría</a>
            <a href="/projects/time-used" class="button">Tiempo por Proyecto</a>
    </div>

    <!-- Mensaje adicional -->
    <div class="message info">
      <p>
        <% if (user) { %>
          ¡Bienvenido de nuevo, <%= user.username %>! Haz clic en "Ir al Dashboard" para comenzar.
            <% } else { %>
              Selecciona una opción para comenzar.
              <% } %>
      </p>
    </div>
  </div>
</body>

</html>